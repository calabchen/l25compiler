program MyApp {

    func add(a, b, c){
        let sum = a+b+c;
        return sum;
    }

    struct Point{
        a,
        b
    }


    main {
        let x : [6] = [1, 2, 3, 4, 5, 6];
        output(x[3]);
        let i = 6;
        while(i >= 0){
            output(x[i]);
            i = i - 1;
        };
    }
}
